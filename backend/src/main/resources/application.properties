# Application Configuration
quarkus.application.name=SpoolTracker
quarkus.http.port=${QUARKUS_HTTP_PORT:9002}

# CORS Configuration for React frontend
quarkus.http.cors.enabled=true
quarkus.http.cors.origins=${QUARKUS_HTTP_CORS_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:3000,https://192.168.2.13:3443,https://192.168.2.13:3000}
quarkus.http.cors.methods=GET,POST,PUT,PATCH,DELETE,OPTIONS
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with
quarkus.http.cors.exposed-headers=location,info
quarkus.http.cors.access-control-max-age=24H

# Database Configuration - MariaDB
# Use environment variables for sensitive data
quarkus.datasource.db-kind=mariadb
quarkus.datasource.jdbc.url=${QUARKUS_DATASOURCE_JDBC_URL:jdbc:mariadb://192.168.2.13:3306/spooltracker}
quarkus.datasource.username=${QUARKUS_DATASOURCE_USERNAME:root}
quarkus.datasource.password=${QUARKUS_DATASOURCE_PASSWORD:}

# Hibernate settings
quarkus.hibernate-orm.database.generation=drop-and-create
quarkus.hibernate-orm.log.sql=false
quarkus.hibernate-orm.physical-naming-strategy=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy

# Seed data already loaded - set to import.sql for fresh DB
quarkus.hibernate-orm.sql-load-script=import.sql

# JSON serialization
quarkus.jackson.serialization-inclusion=non-null

# Dev UI
quarkus.swagger-ui.always-include=true
